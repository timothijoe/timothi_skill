# 完整快照命名确认

`/diary-zt` 不再立即写入文件。无论用户是否传入标题，先解析保存目录、生成标题和时间戳，并显示建议的完整文件名及目标路径。

等待用户在同一会话中作出一个明确选择：

- `确认`：以当前建议创建完整快照。
- 提供新标题：重新生成并展示建议文件名，继续等待确认。
- `取消`：不创建文件，也不记录本次快照为可追加目标。

每次完整快照均执行该确认流程。`/diary-zt-a` 只向已有目标追加增量，不新建文件，因此不要求命名确认。
