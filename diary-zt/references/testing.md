# 跨设备测试

1. 安装整个 `diary-zt` 文件夹后重启目标工具。
2. 运行 `/diary-zt 测试`，确认仅显示建议路径和文件名，尚未创建文件。
3. 回复新标题，确认显示新的建议；回复 `取消`，确认不创建文件。
4. 再运行 `/diary-zt` 并回复 `确认`，确认创建完整快照。
5. 首次快照后发送消息，运行 `/diary-zt-a`，确认只追加新内容；无新增内容时确认不写入。
6. 检查配置目录存在、配置缺失和配置路径缺失三种路径回退行为，并在 Windows、macOS、Linux 上检查用户主目录解析。
